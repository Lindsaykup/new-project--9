let player;
let stars;
let score = 0;

function setup() {
  createCanvas(800, 600);

  // Player setup
  player = new Sprite(width / 2, height - 50, 100, 20);
  player.color = color("blue");

  // Stars setup
  stars = new Group();
}












